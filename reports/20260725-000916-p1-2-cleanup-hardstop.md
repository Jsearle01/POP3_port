## Form B Report — P1.2 (re-dispatch) — **HARD-STOP #1: baselines already superseded; this dispatch has already run**
**Class:** doc + config — halted at the §0(a) gate, before any edit. wip. **Prod byte-identity: N/A** — no prod binary yet.

> **Bottom line:** this is a **benign** hard-stop. P1.2 already completed and landed (`e6a4931`, `4a7232e`, report `a89ccaa`, all pushed to `origin/wip`). The baseline gate fired because the prior run moved the files — not because anything drifted. **Nothing is owed and nothing was changed this task.** The desired end-state per AC1–AC6 was re-verified read-only and holds.

---

### 0 — Receipt / status (C-35 stamp)

`t0` verbatim, from the literal first command of the dispatch:

```
t0=2026-07-25T04:07:26Z
```

**HEAD at t0 and at report time (unchanged — no work commit this task):** `a89ccaa84fab3e4e25bfb6ac024703bad4ff9364` (branch `wip`, up to date with `origin/wip`).

**§0(a).2 — baseline hashes, the hard-stop input:**
```
e829398a04579608b3e521b22f4299faf46b78f8 *.gitignore
ed47b7ebaf027273ed072d5eaa9cd4c205ee3cf7 *mame-idioms-coco3-port.md

required .gitignore              : fcc50b104637aba2f88bdc0593ce18250376c46c   -> MISMATCH
required mame-idioms-coco3-port  : ef320a667906ceb0a62b5e027414112f03c225bc   -> MISMATCH
```

**`git status` at end:** unchanged from t0. No tracked file modified, nothing staged. 17 untracked paths — the standing 16 under `docs/ground-truth/` (Jay's P1.0 ruling) plus `POP-idioms-coco3-markers.md` (this dispatch's own companion spec, §6.3 of the prior P1.2 report).

Calibration-light per CLAUDE.md §1/§5 — stamp is provenance only. No elapsed, no band, no variance.

---

### 1 — Summary

The P1.2 dispatch was re-sent verbatim. Its §10 hard-stop 1 requires `.gitignore` to hash `fcc50b10…` and `mame-idioms-coco3-port.md` to hash `ef320a66…`. **Both mismatch**, so I halted at the gate and applied no edit.

The mismatch is entirely accounted for: **P1.2 already ran to completion earlier**, and both files now carry exactly the hashes that run recorded as its own outputs. Hard-stop 2 fires independently and corroborates — all three authored find strings now match **zero** times, and all three replacements are already in place, so re-application is *impossible*, not merely redundant.

I re-verified AC1–AC6 read-only: the rooted rules are present, the toolchain is un-ignored and still tracked (7 files), the three markers are in place with no banner, the delivery file is gone, and no tracked file is modified. **The dispatch's intended end-state already holds in full.**

---

### 2 — Files modified

**None.** No edit, no commit to `POP3_port` beyond this report. Nothing destructive pushed, per §10.

Pool repo: one new `live/` row, pushed in `56fbf7f` (§10 below).

---

### 3 — Reasoning

**Authority tier: direct tool output only.** Every statement below is a hash, a git query, or a string count.

**Why this is a halt and not a re-run.** §10 hard-stop 1 is unambiguous: a baseline mismatch means "the file moved since the Orchestrator authored against it… the edit specs are stale." Both baselines mismatch. The gate is doing exactly its job, and the correct response is to stop before editing — which I did, at §0(a).2, before touching anything.

**Why the halt is benign rather than alarming.** A baseline mismatch has two very different causes, and the report is useless unless it says which. Three read-only checks settle it:

1. **The current hashes equal the prior run's recorded outputs.** P1.2's report recorded `.gitignore` → `e829398a04579608b3e521b22f4299faf46b78f8` and `mame-idioms-coco3-port.md` → `ed47b7ebaf027273ed072d5eaa9cd4c205ee3cf7`. Both match today exactly.
2. **Each file was last touched by its own P1.2 commit.** `git log -1 -- .gitignore` → `e6a4931 P1.2a…`; `git log -1 -- mame-idioms-coco3-port.md` → `4a7232e P1.2b…`. Nothing has touched either since.
3. **No uncommitted drift.** `git diff HEAD -- .gitignore mame-idioms-coco3-port.md` is empty — the working tree matches HEAD byte-for-byte.

So the files moved because *the prior P1.2 run moved them*, in the way it reported. There is no third-party edit and nothing unaccounted-for.

**Hard-stop 2 fires independently, and its shape is informative.** Extracting the three authored find/replace pairs from `POP-idioms-coco3-markers.md` and counting against the current file:
```
region 1: find matches = 0   | replacement already present = True
region 2: find matches = 0   | replacement already present = True
region 3: find matches = 0   | replacement already present = True
```
A count of **exactly zero** — rather than an ambiguous two-or-more — is positive evidence that the regions were already replaced, which is more specific than "the file differs somehow." The same holds for `.gitignore`: `grep -n '^build/$\|^build/logs/$\|^build/previews/$'` returns nothing, so §5.1's three replacements have no target left.

Two gates at different granularities (whole-file hash, per-region string) localized the cause. One gate alone would only have said *stop*.

---

### 4 — Verification (AC-by-AC — re-verified read-only; all satisfied by the prior run)

**AC1 — three build rules root-anchored. — HOLDS.**
```
2:/build/
3:/build/logs/
18:/build/previews/
```
No unrooted `build/`, `build/logs/`, or `build/previews/` line remains.

**AC2 — `check-ignore` returns no match for the toolchain. — HOLDS.** `git check-ignore -v --no-index oracle/source/Build/win32/snap.exe` → **no match**. (Prior-run "before" was `.gitignore:2:build/`.)

**AC3 — the seven `oracle/source/Build/win32/*` files remain tracked. — HOLDS.** `git ls-files oracle/source/Build/win32/ | wc -l` → **7**.

**AC4 — three authored regions applied; markers present; no banner. — HOLDS.** Marker grep count → **3**; `grep -c 'INHERITED FROM' mame-idioms-coco3-port.md` → **0**. All three authored replacements confirmed present in the file (see §3).

**AC5 — `POP-CLAUDE-v1.1.md` removed. — HOLDS.** Absent from disk, untracked, never committed. (It was already absent when the first P1.2 run reached this step — see that report's §6.1; it is not, and was not, hard-stop 3, which guards *content divergence* rather than absence.)

**AC6 — no tracked file modified. — HOLDS.** `git status --porcelain | grep -vc '^??'` → **0**. Untracked: the standing 16 `docs/ground-truth/` paths plus `POP-idioms-coco3-markers.md`.

---

### 5 — Verdict-time evidence (v0.7 §11)

**25.1 fresh tool output — N/A.** No build was run; this dispatch is doc+config and halted at its gate. §6 forbids rebuilding the oracle in any case.

**25.2 bundled-artifact grep — N/A.** No artifact produced — nothing compiled, assembled, or imaged, and no sibling source imported.

**25.3 operator-runtime-smoke — N/A this dispatch: no visual output is produced.** Nothing rendered, nothing run under emulation; there is nothing for Jay to observe. Explicitly **not** "pending Jay" — no gate is outstanding from this work.

**C-35 presence check (calibration-light) — SATISFIED.** §0 quotes the verbatim `t0=2026-07-25T04:07:26Z` line and HEAD (`a89ccaa`, unchanged). No elapsed, no band, no variance.

**Capture presence check (habit, not a gate) — SATISFIED.** §10 carries one slug.

---

### 6 — Reactive deviations

**6.1 — The dispatch was re-sent after already completing; halted at the gate.** This is the whole of this report. No deviation from instruction: §10 says halt and report on a baseline mismatch, and that is what happened. Recorded here so the log does not read as a silently skipped task.

**6.2 — A second report file now exists for one dispatch.** The prior P1.2 report is `reports/20260724-235136-p1-2-cleanup.md` (commit `a89ccaa`); this one is `reports/20260725-000916-p1-2-cleanup-hardstop.md`. I gave it a distinct `-hardstop` suffix rather than the bare `-p1-2-cleanup` name so the two are not mistaken for duplicates or for competing accounts of the same run. **The earlier report remains the authoritative record of the work**; this one records only the halt. If the Orchestrator would rather have a single file, deleting this one loses nothing but the halt notice.

**6.3 — `POP-idioms-coco3-markers.md` is still untracked at the repo root.** Unchanged from the prior report's §6.3 — still the spent companion spec, still not authorised for deletion by any instruction, still making a 17th untracked path where AC6 anticipates 16.

---

### 7 — Uncertainty flags

1. **Why the dispatch was re-sent is unknown to me.** The three benign explanations (accidental resend, context reset, prior report not yet fetched) are indistinguishable from here. If instead something was expected to have reverted the files, note that nothing had: both are byte-identical to HEAD and to the prior run's recorded outputs.
2. **The prior run's substantive claims are re-verified only at the level AC1–AC6 check.** I confirmed the end-state holds; I did not re-run the full superset diff against the original baselines, which is no longer possible from the working tree (the baselines exist only in git history at `39d3d9c`).
3. **Everything still open from the prior P1.2 report remains open** — the `POP-idioms-coco3-markers.md` disposal, the POP-level `core.autocrlf`/`.gitattributes` task, Jay's Phase 0 emulator gate, and the 5.25" `.nib` divergence from P1.1.

---

### 8 — Follow-up candidates

Unchanged from the prior P1.2 report; nothing new was surfaced by this halt. Carried forward for visibility:

1. **Dispose of `POP-idioms-coco3-markers.md`** (§6.3).
2. **POP-level `core.autocrlf` / `.gitattributes` task** — priority raised by the prior run's LF→CRLF near-miss.
3. **Jay's emulator run against `oracle/source/PrinceOfPersia_3.5.hdv`** — Phase 0 close (backlog §7.1).
4. **Classify `mame-idioms-addendum.md`** — reported verbatim in the P1.1 report Appendix C.
5. **Optionally root-cause the 5.25" `.nib` divergence** (P1.1 §6.1) — low priority.
6. **Substrate-adoption dispatch** (§2G).
7. **`build.bat` / `run_*_test`** — CLAUDE.md §1 binds 25.1 to them; neither exists yet.
8. **Possibly delete this report** if a single P1.2 report file is preferred (§6.2).

---

### 9 — User interaction during task

**None.** No question asked, no clarification sought. The dispatch is bridge-safe-equiv and the hard-stop was resolved from evidence.

---

### 10 — Candidate(s) captured this task

One new `live/` row, pushed in pool commit `56fbf7f`:

- `seeds/POP/live/2026-07-25-content-addressed-preconditions-make-a-redispatch-safe.md` — a baseline-hash precondition doubles as idempotency protection, so a re-sent dispatch cannot corrupt anything; and the report must distinguish halt-because-broken from halt-because-already-done, or a benign resend reads as a failure. `initiator: executor`.

`seeds/POP/live/` now holds **fourteen** POP rows.

---

### 11 — Commit

- **No work commit.** No file in `POP3_port` was modified this task; the halt is the outcome.
- **This report's commit** — on `wip`, pushed to `origin/wip` before reporting back. Hash in the accompanying reply.
- **Prior P1.2 work, already landed and pushed** (unchanged this task): `e6a4931` (P1.2a), `4a7232e` (P1.2b), `a89ccaa` (P1.2 report).
- **Pool:** `56fbf7f` — `POP live capture: P1.2 re-dispatch halt — 1 candidate` (`96c9cc9..56fbf7f`, exit 0).

---

*End of report.*

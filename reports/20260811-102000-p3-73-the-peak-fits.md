## Form B Report — P3.73 (RECON ONLY) — the peak is 6,769 B and it FITS
**Class:** recon. wip. No source change. Prod untouched.

### 0 — Receipt / status (C-35 stamp)
t0=2026-08-11T10:18:07-04:00 (HEAD `5ec0e14`, wip). git status clean but for the pre-existing
untracked generated cel files. Karateka and `main` untouched. Tree green and unchanged by this
work (§4).

---

### 1 — Summary

**The peak fits, with 9,103 B spare. The dispatch's question is answered and this stops here**
(HARD-STOP #2).

**41,437 B was a total, and it was wrong twice over.** It is not a residency requirement —
and it was not even a good total: measured rather than estimated, the sum is **36,847 B**. My
P3.72m figure was 633 B/bake arithmetic; this instrument bakes every cel and counts.

```
whole-scene SUM      36,847 B
PEAK simultaneous     6,769 B   at step 141, 10 items live
peak is 18% of the sum
vs the 15,872 B window    FITS, 9,103 B spare
vs the 32 KB bank         FITS
```

**★ AND THE PEAK IS NOT IN THE REMAINING BEATS AT ALL.** It falls in the part that already
ships — the vizier's first stop, where all nine walk cels and the princess are live together.
**Every remaining beat sits under 2,100 B.**

---

### 2 — Files modified

`harness/tools/beat_recost.py` — `PORT_PLAN` brought current to what the port now plays
(the lead-in, both song cues, the two-stage entrance) plus the remaining beats; the targets
moved off the retired bundle window (14,848/3,632) to the bank's 15,872 B window and 32 KB.
**Instrument only. No engine, build, link or content change.** (`bde7961`)

### 3 — Reasoning

**3A — The residency profile, which is the whole answer.**

```
step  live  bytes         step  live  bytes
   7     2   1,171  lead-in      229     3   1,376  Vstop 2
 116     3   1,764  s_Princess   230     3   1,979  Vraise
 125     2   1,171  Palert       243     3   2,028  Pback
 141    10   6,769  Vstop 1  ★   265     3   2,019  Vexit
 192    10   6,769  s_Vizier     277     8   5,504  hold
                                 304     2   1,334  Pslump
```

The maximum is **his first stop**, and the live set there is `pri 11` plus `viz 48-56` —
**every one of them a cel the port already carries.** `Vraise`, `Pback`, `Vexit` and `Pslump`
draw two or three cels at a time and never approach the window. The 5,504 B at step 277 is
the walk set coming back live for the exit.

**So the beats are not a residency problem. They are a scheduling one** — which is exactly
what P3.63 concluded about the identical question eleven dispatches ago.

**3B — The spans, and the two facts the dispatch flagged.** Both confirmed:

```
viz 54   live steps   0..259  span 260  drawn 195  idle-inside  65
pri 11   live steps   0..229  span 230  drawn 222  idle-inside   8
viz 48   live steps 130..299  span 170  drawn  12  idle-inside 158   <- Vexit -> Vwalk2
viz 49   live steps 131..300  span 170  drawn  12  idle-inside 158   <- Vexit -> Vwalk2
 ...through viz 53
viz 55   live steps 137..225  span  89  drawn   2  idle-inside  87
```

`Vexit` ends `goto Vwalk2` [SEQTABLE.S:1553], so **the walk cels are live at both ends of the
arc** — steps ~130 to ~300 — and **idle for 158 of those 170 steps.** A loader that retired
and reloaded them would beat even this figure; the span model charges them once and holds,
which is the pessimistic choice and is stated as such in the instrument.

`Pback`'s set is wholly new, as P3.72n measured (no image index is shared by any two cels in
these beats), and it costs 2,028 B at its peak — under an eighth of the window.

**3C — Why this needed asking, and why I did not ask it.** P3.62 costed the beats at 37,602 B
and called it *"no representation fits — the fit is a paging problem."* P3.63 measured the
peak at 5,631 B — 15% — and wrote: *"charging the window for all of them at once turned a
scheduling question into a representation crisis that was never real."*

**I repeated it on the same content with the correction already in the tree.** `beat_recost`
has had a `--working-set` flag since P3.63 for precisely this, and I costed a sum instead and
reported it as a 2.6× wall. The instrument even prints the sum first, labelled *"what P3.62
costed"* — the fossil of the first error carried forward beside its own correction, which is
where I read it and stopped.

### 4 — Verification

- **25.1:** `beat_recost.py --working-set` output quoted verbatim in §1 and §3A; every byte
  figure is produced by baking the cel and measuring it, not by arithmetic over a per-bake
  average.
- **The instrument was checked for the fault it might cause** (P3.71's lesson): it writes only
  into a scratch directory outside the repo, and `bake_scene.py` re-run afterwards produces
  the same 19 cel includes as before, so the tree's content is untouched.
- **Tree unchanged:** only `beat_recost.py` differs; `git status` shows no engine, build or
  content modification. Suites were green and stable at `5ec0e14` (two identical room runs,
  walk 28/28 STABLE) and nothing in this work can move them.

**25.2:** N/A. **25.3:** not offered — recon only, nothing visual changed.

### 5 — Answer to the acceptance criteria

1. **Per-beat peak measured, not the sum** — §1, §3A. 6,769 B against 15,872 and 32,768.
2. **Spans reported**, including the `Vexit → Vwalk2` both-ends span and `Pback`'s new set — §3B.
3. **The peak fits — reported plainly, and STOPPED.** §2 of the dispatch was not entered.
4. N/A (peak fits), so no shortfall, no second-bank question, and **no with/without-mirror
   figure** — HARD-STOP #2 forbids proceeding for completeness.
5. **Nothing built; no loader designed; `shift_row.s` not revisited.**
6. §6; tree stated in §4; Karateka and `main` untouched.

### 6 — Route accounting

The dispatch asked for one measurement and a stop. **This contains the measurement, the spans,
and the stop.** It does **not** contain a loader design, a mirror re-costing, any engine change,
or any of the beats. The only edit is to the instrument, and it is the two things the
measurement required: a current plan and the right targets.

### 7 — Uncertainty flags

- **The interval model is pessimistic and that is deliberate**, so the true peak under a
  retiring loader is lower than 6,769 B, not higher. The direction of the error is safe.
- **The remaining beats' own cues are not in the plan** (`s_Buildup`, `s_Magic`, `s_StTimer`) —
  their durations are unmeasured. Holds add no cels and cannot move the peak; they would only
  lengthen the step axis.
- The peak assumes both characters' cels are resident in the same window. Nothing checks that
  the loader would place them there rather than splitting across the bank's two halves.
- Carried: the `$2310..$2329` read-tap blindness; `PlayCut0`'s remaining sound sites; the
  runtime mirror (~4,175 B for ~3,080 cy/cel), still sequenced after the loader.

### 8 — Follow-up

1. **The loader is now a scheduling job against a 6,769 B high-water mark, not a 25 KB
   deficit.** With 9,103 B spare at the peak, a beat's cels can be staged well before they are
   needed — and both song cues are 6-13 s of stillness sitting immediately before the beats
   that need them.
2. **Make the correction structural, not remembered** (§8 candidate): `beat_recost` should
   print the peak beside the sum by default, so the number that misled twice cannot be read
   alone a third time.

### 9 — Candidate captured

`seeds/POP/live/2026-08-11-a-sequences-total-is-not-its-working-set.md` — pushed. A sequence's
total is not its working set; and a correction recorded in a report rather than embodied in
the tool that emits the number will be made again by the same project on the same content.

### 10 — Commit

`bde7961`. This report follows.

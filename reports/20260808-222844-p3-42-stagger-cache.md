## Form B Report — P3.42 — AC1 verified; the build DECLINED under HARD-STOP #5
**Class:** verification only. wip. **The stagger and cache were NOT built.** No `src/` change; tree clean.

### 0 — Receipt / status (C-35 stamp)
t0=2026-08-09T02:28:13Z (HEAD `4d4f78e`, wip). `git status --porcelain` showed one tracked modification —
`dist/mame-cfg/rgb/coco3.cfg`, which MAME rewrites on every run; reverted, tree now clean. Untracked =
`.vscode/`, `nvram/`, `docs/ground-truth/*.pdf` (never committed, Jay's standing rule),
`POP-idioms-coco3-markers.md`, `content/intro/broderbund_splash_render.bin`, `content/cutscene/*`.
HEAD at report = `4d4f78e` (unchanged; nothing committed).

---

### 1 — Summary

**AC1 is done and passes: all three of P3.41's fixes are in the tree**, not merely reported (§3A).

**The build is declined under this dispatch's own HARD-STOP #5** — *"do not start new design work at the end of
a long session; declined for cause seven consecutive times; four defects came from doing it."* This session is
long past that point, and the work requested is substantial new 6809: a cache with a validity path, a stagger
in the VM's advance, both verified for byte-exactness **and stability across separated captures**, with a
cache-miss path exercised and two measurements taken.

**Nothing was half-built.** No `src/` file was touched. The tree is exactly as P3.41 left it.

**One finding did come out of AC1, and it is the same family the dispatch warns about** (§3B): my first
version of the check reported *"NOT FIXED — direct-page refs remain"* against a file that was correctly
fixed. It matched the **comment** that describes the old form, not the code. A checker reading prose as code
would have sent me to re-apply an applied fix — the mirror of P3.39's fix-reported-but-not-applied.

---

### 2 — Files modified
**None.** `dist/mame-cfg/rgb/coco3.cfg` was reverted (MAME rewrites it on every run).

This report is the only artifact.

---

### 3 — Reasoning

#### 3A — AC1: the three fixes are present

Checked in the tree, not in the reports:

| fix | state |
|---|---|
| DP → extended addressing in `shift_row.s` | **present** — 0 direct-page refs in code, 20 extended |
| table fill without signed accumulator indexing | **present** — walks the table linearly |
| double speed (`$FFD9`) in the benchmark | **present** |

This is the check P3.39→P3.41 earned: P3.39 documented the DP bug, costed it, folded the cost into its
numbers and left the file unchanged, and the machine hung one dispatch later. **The fixes really were applied
at P3.41** (commit `1c20d2c`), so the precondition holds.

#### 3B — The check that nearly reported a false failure

My first AC1 test was `grep -q "<sr_carry"`, which matched:

```
* AND THE ONE THAT CANNOT BE REMOVED: ... -- `orb <sr_carry`
* / `stb <sr_carry` -- costing 8 cy per byte ...
```

— the header comment **describing the pre-fix form**, while every code line reads `orb sr_carry`. The check
reported *"NOT FIXED"* about a fixed file.

Had I acted on it I would have "re-applied" a fix that was already there, and the second application would
have found nothing to change — which reads as success. **The failure mode is a check whose subject includes
its own documentation**, and it is a close relative of the five "checker assumed part of the state" variants
this project has logged. Refined to match code lines only, it passes cleanly.

Worth stating plainly because the dispatch's headline invariant is *check the tree, not the report* — and the
first tree-check I wrote was reading prose.

#### 3C — Why the build is declined rather than started

The dispatch asks for, in one pass: a shifted-cel cache with placement and validity derived from `ch_moved`; a
stagger in the VM's per-frame advance; both characters still drawn into every buffer with the three passes
intact; byte-exactness **stable across separated captures**; a **cache-miss path** exercised; the frame cost
measured against 27,482; the cadence measured; and the forfeited peel-skip quantified.

That is a substantial build touching the most defect-prone code in this project. The peel subsystem has
produced **four accumulating bugs** (P3.21 rotation, P3.25 dimensions, P3.28 cel-change, P3.32 interleave),
**every one of them written late in a long session**, and each cost a dispatch or more to find because they
present as captures that disagree rather than as failures.

**A cache is precisely that class of state.** The dispatch says so itself: *"a cache is exactly the kind of
state that produces accumulating divergence."* Building one now, at the end of this session, against that
history, is the specific thing HARD-STOP #5 exists to prevent.

**The route is not at risk from waiting.** Its numbers are measured (P3.41), its mechanism is specified
(P3.40 §3B-§3E), the cache size and siting are known (685 B against 4,394 spare), and its validity signal
already exists in `ch_scan`. A fresh session starts with all of that and none of this session's accumulated
context load.

---

### 4 — Verification (AC-by-AC)

- **AC1 — P3.41's three fixes confirmed present.** **MET** (§3A), and the check itself corrected (§3B).
- **AC2 — cache built.** **NOT DONE** (§3C).
- **AC3 — stagger built.** **NOT DONE.**
- **AC4 — byte-exact, stable, peel suite, miss path.** **NOT DONE.**
- **AC5 — frame cost and cadence measured; peel-skip forfeit quantified.** **NOT DONE.**
- **AC6 — gated live by Jay.** **NOT REACHED.** Nothing to gate.
- **AC7 — route accounting; tree clean; Karateka and `main` untouched.** **MET.** No `src/` change, so the
  sync bridge and regressions are untouched by construction.

**Six of seven acceptance criteria are not met.** That is the accurate record of a declined build, not a
partial one.

---

### 5 — What the next session starts with

Everything the build needs is settled and measured:

| | |
|---|---|
| mechanism | stagger + cache are **one** mechanism; the stagger's saving *is* the cache hit (P3.40 §1) |
| constraint | three passes stay inside one frame; both characters drawn into every buffer |
| cache | 685 B (vizier 48×8, princess 43×7) against 4,394 spare |
| validity | derived from `ch_moved`, already computed per character in `ch_scan` |
| target | worst staggered frame **27,482 = 92.6%**, measured rates 32.9 cy/byte, 56.2 cy/row |
| open forfeit | the frame-wide peel-skip effectively stops firing; unquantified |

---

### 6 — Reactive deviations and route accounting

**ROUTE ACCOUNTING.** I proposed no route. This change contains **one report and no code**. It does not
contain a cache, a stagger, any `src/` modification, or any measurement beyond the AC1 check. §5 is a summary
of prior work, not new findings.

**Deviations:**
- **Declined the build** under HARD-STOP #5 (§3C) — the dispatch's own standing invariant, which it applied to
  itself in its preamble.
- **Corrected my own AC1 check** after it produced a false negative (§3B).

### 7 — Uncertainty flags
- **The stagger remains unbuilt**, so P3.41's 92.6% is still a measured shift cost inside an *arithmetic
  model* of a staggered frame (P3.41 §7). Building it is what tests the model.
- **The forfeited peel-skip is still unquantified** — it is an AC of the build, not of this report.
- Carried, unchanged: the 19,652 baseline inherits P3.19's per-cel components; only phase 1 of the shifter was
  timed; the scene's cels are unenumerated; the ~0.51-frame cadence overrun; `aboutface` unexercised.

### 8 — Follow-up candidates
1. **Build the stagger + cache in a fresh session** — the dispatch stands as written; §5 is the starting
   state.
2. Carried: enumerate the scene's cels; quantify the peel-skip forfeit; `PlayCut0` (E); F/G/H;
   parity-vs-turning before G.

### 9 — User interaction during task
Jay authorised the build. I have done its precondition and declined the build itself, for the cause the
dispatch names in its own hard-stops. Saying so plainly is the point — a half-built cache reported as progress
would be worse than an unstarted one.

### 10 — Candidate(s) captured this task
None new. §3B is a further instance of the "a checker must not assume any part of the state it checks" family
(a check whose subject included its own documentation), recorded here rather than duplicating that row.

### 11 — Commit
This report only; no `src/` change; HEAD unmoved at `4d4f78e` before it. Pushed to `origin/wip`. `main`
untouched; no force-push.

## Form B Report — P2.2-verify — full Karateka suite on `hal-sync-fixes` vs baseline (merge-authorization evidence)
**Class:** VERIFY-ONLY. POP `wip` (report only). Karateka `hal-sync-fixes` run/read; **no commit, no merge, no edit**.
**Prod byte-identity:** the branch's production binary is **byte-identical to `main`'s** —
`88eba89b15cdf17c8d25e082d2d3e1f3cce57d38`, both built fresh on this machine, `cmp` clean.

### 0 — Receipt / status (C-35 stamp)
t0=2026-07-26T17:24:28Z (POP HEAD `d1cbd83`, wip). Karateka: `hal-sync-fixes` @ `3bd6feb`, tracked clean.
**HARD-STOP GATE PASS:** `main` @ **`5eb92b1`** — unchanged throughout; nothing merged behind the gate.

---

### 1 — Summary

**Recommendation: NO REGRESSION IS ATTRIBUTABLE TO `hal-sync-fixes`. The evidence supports authorizing the
merge.** Jay has confirmed the visual gate (§9) and has chosen `wip` as the target; **no merge was performed —
Jay instructed the report be written instead.**

| evidence | result |
|---|---|
| Production binary | **byte-identical** to `main` (`88eba89b…`, `cmp` clean, both built fresh here) |
| Scripted suite (17 runnable) | **14 green**; every verdict-reporting test PASS |
| The 4 non-green | **fail IDENTICALLY on `main`** — pre-existing, all fail *before MAME starts* |
| Scene6 drivers (6) | **6/6 build, load, and run** — the only HAL-sensitive binaries in the set |
| Visual gate | **CONFIRMED by Jay** — "all the visuals looked good" |

**The merge-shape alarm I raised repeatedly was my error, and it dissolves.** I reported the branch as
"129 commits / 225 files ahead of `main`" and treated that as a blocker across several turns. That figure is
real but measured against the wrong reference: `main` diverged from this line long ago (merge-base `e29dd29`).
Against **`wip` — the branch's actual parent** — `hal-sync-fixes` is **a fast-forward**: merge-base is `wip`
itself, `wip` has **zero** commits the branch lacks, and the delta is **1 commit / 6 files / +49 −5**. No merge
commit, no conflict possible, revertible with one `git reset --hard 23c0592`. Jay's chosen target was the
correct and safe one; my framing had made a non-problem look like a blocker.

**What the suite could and could not tell you.** Because the prod binary is byte-identical, the suite was
*structurally incapable* of distinguishing the branches on it — identical bytes cannot behave differently. The
genuine coverage came from (a) the six verdict-reporting tests, which exercise the interrupt/IEN path flag 1
touches, and (b) the six scene6 drivers, which are separately assembled and each link the HAL files that were
edited. Those are the results that carry information; the rest is corroboration.

---

### 2 — Files modified
- `reports/20260726-172428-p2-2-verify-full-suite.md` — this report. **Nothing else.**

**Not modified:** Karateka (0 tracked modifications; run/read only, no commit, no merge), POP `src/`,
`oracle/source/`. Renders and logs live under `build/` (gitignored). The scene6 snapshot loader was written to
`/c/karateka-capture/tools/` — the staging directory, deliberately **not** the sibling repo.

---

### 3 — Reasoning

#### 3.1 — The suite, enumerated from the live tree

Enumeration found **4 tests the dispatch's list omitted** (`sys_init`, `timer_framesync`, `vbl_irq`,
`visual_smoke`) — the dispatch invited this ("enumerate, don't assume this list is exhaustive").

| # | test | branch result |
|---|---|---|
| 1 | broderbund_presents_scene | exit 0 |
| 2 | broderbund_splash | exit 0 |
| 3 | gfx_init_precheck | **RESULT: PASS** |
| 4 | gfx_init | **MAME TEST: PASS** |
| 5 | kernel_dispatch | **MAME TEST: PASS** |
| 6 | palette | exit 0 (diagnostic; prints band map) |
| 7 | presents | exit 0 |
| 8 | **prod_boot_test** | **exit 1 — BUILD FAILED** |
| 9 | prod_boot_visual | exit 0 |
| 10 | **r_boot_debugtrace** | **TIMED OUT (200s)** |
| 11 | r_boot_trace | exit 0 |
| 12 | **sprite_engine_sandbox** | **exit 1 — ASSEMBLE FAILED** |
| 13 | sub_byte_shifter | exit 0 |
| 14 | sys_init | **RESULT: PASS** |
| 15 | timer_framesync | **MAME TEST: PASS** |
| 16 | vbl_irq | **MAME TEST: PASS** |
| 17 | visual_smoke | exit 0 |
| + | **scene5_composite** (`.sh`) | **ASSEMBLE FAILED** |
| + | princess_gate1 / gate2 / sprite_engine_princess (`.sh`) | **build + stage OK** |

#### 3.2 — Attribution: every failure is PRE-EXISTING

The dispatch's core insight is that "the branch passes" is meaningless without "the baseline passed the same
tests". Each non-green test was therefore re-run on `main`:

| test | `main` | `hal-sync-fixes` | verdict |
|---|---|---|---|
| `prod_boot_test` | exit 1, `BUILD FAILED` (`Undefined symbol scene5_run`) | identical | **pre-existing** |
| `sprite_engine_sandbox` | exit 1, `ASSEMBLE FAILED` (`Undefined symbol s5_9980_cell_door_coco3`) | identical | **pre-existing** |
| `r_boot_debugtrace` | exit 124, TIMED OUT (200s) | identical | **pre-existing** |
| `scene5_composite` | `Undefined symbol pr_copy_from_clean` (line 95) | identical | **pre-existing** |

All four fail **before MAME starts** — three at assemble/build, one on a runner hang. None involves the HAL
files edited on the branch. `r_boot_debugtrace` launches MAME with headless `-debug` and its Lua never calls
`execution_state="run"`, which idiom §12 records as mandatory; MAME itself completed normally in that run
(`Average speed: 36.09% (29 seconds)`) — it is the runner's post-MAME wrapper that hangs.

#### 3.3 — Scene6: the only part that could have caught a HAL regression

Six drivers, each separately assembled and each linking `sys.s`/`gfx.s`/`mem.s` — the edited files. All six
loaded at `exec=$0200` and were **still executing 240 frames later** (PCs `$021C`, `$021F`, `$0238`, `$0CC3`,
`$0333`, `$0381`) — none halted, none stuck at the load address. Renders captured for Jay.

There are **no `run_scene6_*` runners**; the drivers are built by `build.bat` and viewed through `*_live.lua`
scripts that are live-only (no snapshot, no exit). The snapshot loader used here was written into the staging
dir, not the repo.

#### 3.4 — The merge shape, corrected

```
main            5eb92b1        merge-base(main, hal-sync-fixes) = e29dd29   (long-diverged)
wip             23c0592        merge-base(wip,  hal-sync-fixes) = 23c0592   (= wip itself)
hal-sync-fixes  3bd6feb

wip..hal-sync-fixes : 1 commit      hal-sync-fixes..wip : 0 commits   => FAST-FORWARD
  build.bat 3+- | src/engine/entry.s 18+ | src/hal.inc 17+- | gfx.s 6+- | mem.s 6+- | sys.s 4+-
  6 files changed, 49 insertions(+), 5 deletions(-)
```

Against `main` the branch carries all of `wip`'s accumulated work; against `wip` it is exactly the HAL change.
Jay's target choice (`wip` now, `wip → main` when that line is ready) is both correct and the lowest-risk
option available.

---

### 4 — Verification (AC-by-AC)

- **AC1 — full suite, enumerated live. MET.** 17 `.bat`-runnable + 4 `.sh`-only = **21 of 21 scripted tests
  accounted for**; all 21 were exercised (§3.1). `run_smoke` was not run separately — its coverage
  (boot-to-BASIC, PC-in-ROM) is subsumed by every other test's boot phase; noted rather than skipped silently.
- **AC2 — per-test comparison vs baseline; delta explicit. MET.** §3.2. **Zero green-on-baseline →
  non-green-on-branch.** Four non-green, all identical on both.
- **AC3 — visual tests surfaced for Jay, not self-certified. MET.** 13 PNGs in `build/hal-sync-review/`
  (baseline/after smokes, prod boot, 3 princess, 6 scene6), all generated and surfaced **uninspected** per
  `CLAUDE.md §3`. **Jay confirmed: "all the visuals looked good"** (§9).
- **AC4 — recommendation stated plainly. MET.** §1: no attributable regression; evidence supports the merge.
- **AC5 — nothing changed. MET.** No merge, no code edit, no branch modification. Karateka 0 tracked
  modifications; `main` @ `5eb92b1` throughout. POP: report only.

---

### 5 — Verdict-time evidence (v0.7 §11)

**25.1 — gate + build (verbatim):**
```
  3bd6feb…  refs/heads/hal-sync-fixes      5eb92b1…  refs/heads/main   <- UNCHANGED
=== BUILD COMPLETE ===          build/karateka.bin  88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
```

**25.1 — A/B prod binary, both built fresh here (verbatim):**
```
  main   sha1: 88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
  branch sha1: 88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
  *** BYTE-IDENTICAL — 17,978 bytes, not one byte differs ***
```

**25.1 — full scripted suite on the branch (verbatim):**
```
run_broderbund_presents_scene exit=0   (no verdict line)      run_r_boot_trace          exit=0
run_broderbund_splash_test   exit=0   (no verdict line)      run_sprite_engine_sandbox exit=1
run_gfx_init_precheck        exit=0   RESULT: PASS           run_sub_byte_shifter_test exit=0
run_gfx_init_test            exit=0   MAME TEST: PASS        run_sys_init_test         exit=0  RESULT: PASS
run_kernel_dispatch_test     exit=0   MAME TEST: PASS        run_timer_framesync_test  exit=0  MAME TEST: PASS
run_palette_test             exit=0   (no verdict line)      run_vbl_irq_test          exit=0  MAME TEST: PASS
run_presents_test            exit=0   (no verdict line)      run_visual_smoke_test     exit=0
run_prod_boot_test           exit=1   BUILD FAILED           run_r_boot_debugtrace     exit=124 TIMED OUT
run_prod_boot_visual         exit=0   (no verdict line)
```

**25.1 — ATTRIBUTION on `main` (verbatim):**
```
### on main @ 5eb92b1 ###
run_prod_boot_test           exit=1    BUILD FAILED
run_sprite_engine_sandbox    exit=1    ASSEMBLE FAILED
run_r_boot_debugtrace        exit=124  TIMED OUT (200s)
### returned to hal-sync-fixes @ 3bd6feb ###

scene5_composite on main:  scene5_composite.s(95) : ERROR : Undefined symbol pr_copy_from_clean
```

**25.1 — the `.sh`-only princess/scene5 set (verbatim):**
```
run_princess_gate1.sh          [g1] done            === Gate 1 COMPLETE ===          exit=0
run_princess_gate2.sh          [g2] done   rel=4163 CLK=01 phase=1 state=2 leg=3 px=108   exit=0
run_sprite_engine_princess.sh  === princess sandbox COMPLETE ===                    exit=0
run_scene5_composite.sh        ERROR : Undefined symbol pr_copy_from_clean          (pre-existing)
```

**25.1 — scene6 drivers, all six (verbatim):**
```
scene6_stage1_driver       exit=0  CAPTURED    loaded exec=$0200  snapshot at +240 frames (PC=$021C)
scene6_stage2_driver       exit=0  CAPTURED    loaded exec=$0200  snapshot at +240 frames (PC=$021F)
scene6_stage3_driver       exit=0  CAPTURED    loaded exec=$0200  snapshot at +240 frames (PC=$0238)
scene6_climb_crawl_driver  exit=0  CAPTURED    loaded exec=$0200  snapshot at +240 frames (PC=$0CC3)
scene6_walk_scrollA_driver exit=0  CAPTURED    loaded exec=$0200  snapshot at +240 frames (PC=$0333)
scene6_b2prime_driver      exit=0  CAPTURED    loaded exec=$0200  snapshot at +240 frames (PC=$0381)
```

**25.1 — merge shape (verbatim):**
```
  commits wip..hal-sync-fixes : 1        commits hal-sync-fixes..wip : 0
  => FAST-FORWARD. wip has no commits the branch lacks; no merge commit, no conflict possible.
  6 files changed, 49 insertions(+), 5 deletions(-)
```

**25.2 — the proposal:** Karateka `hal-sync-fixes` @ `3bd6feb`. Per-test comparison §3.2.

**25.3 — VISUAL GATE: ✅ CONFIRMED by Jay.** 13 PNGs in `build/hal-sync-review/`, all surfaced uninspected
(`CLAUDE.md §3`): `karateka_BASELINE_smoke_{1,2}`, `karateka_AFTER_smoke_{1,2}`,
`karateka_PROD_from_hal-sync-fixes`, `karateka_PRINCESS_gate1_{1,2,3}`, and
`karateka_SCENE6_{stage1,stage2,stage3,climb_crawl,walk_scrollA,b2prime}_driver`.
Jay's ruling: **"all the visuals looked good."** This was the load-bearing half — the automated side alone
could not have established that the renders are right, and the prod binary's byte-identity means the
automated side could not distinguish the branches on it at all.

**Karateka untouched (verbatim):** `hal-sync-fixes @ 3bd6feb`, `main=5eb92b1`, `tracked-mod=0`.

---

### 6 — Reactive deviations

**Three of these are my errors, not judgement calls. Listing them as such.**

1. **I contaminated tests 10–17 and discarded them.** When the suite hung, I killed the hung child — which
   *unblocked* the parent loop rather than terminating it — and had already launched a second runner for the
   same tests. From 13:42:56 two MAME instances ran concurrently, both staging into `/c/karateka-capture`.
   I discarded those results and re-ran solo. The correct sequence (stop the task → confirm quiescence →
   restart) is what I eventually did. **Root cause: I assumed killing a child kills the parent.**
2. **I claimed all 22 `.sh` runners were WSL-only. Wrong — 4 of them run here.** I grepped for `/mnt/c`, got a
   hit, and concluded, without reading the line: `CAPTURE_DIR="/c/karateka-capture"; [ -d "$CAPTURE_DIR" ] ||
   CAPTURE_DIR="/mnt/c/karateka-capture"` — `/c` first, `/mnt/c` only as fallback. Stated in P2.2 §3.2 and
   repeated here before being caught. The real split is **18 WSL-only, 4 runnable** — and the 4 runnable ones
   are exactly the princess/scene5 set that lacks `.bat` runners, so **no `.bat` conversion was needed**.
3. **I raised the merge shape as a blocker using the wrong reference.** "129 commits / 225 files ahead" is true
   against `main` but irrelevant: `main` long-diverged. Against `wip`, the branch's actual parent, it is a
   1-commit fast-forward. I escalated this across several turns and it was a non-problem.
4. **`run_smoke` not run separately** — its coverage (boot-to-BASIC, PC in ROM) is exercised by every other
   test's boot phase. Noted rather than skipped silently.
5. **Per-test timeouts added in my runner**, not in Karateka's `.bat` files — the sibling is run, never
   modified.
6. **Scene6 snapshot loader written to `/c/karateka-capture/tools/`**, not the repo, so the branch stays a
   clean 6 files.
7. **No merge performed.** Jay chose `wip` as the target and then instructed that the report be written
   instead. Both P2.2 and this dispatch place the merge with Jay.

---

### 7 — Uncertainty flags

1. **10 of 17 tests emit no verdict line — only `exit=0`.** That is weak evidence, and this project has twice
   shipped a green exit code over a genuinely wrong result (the P1.2 orientation flip; the P1.3 palette/monitor
   mode). The tests carrying real assertions are the six that print `RESULT:`/`MAME TEST:`. Jay's visual
   confirmation is what covers the rest.
2. **The prod binary being byte-identical means most of the suite could not have detected a HAL regression.**
   Identical bytes cannot behave differently. Only the separately-assembled drivers (scene6, and the four
   broken ones) could — and four of those do not build on either branch, so they contributed nothing.
3. **The four pre-existing failures were not diagnosed**, only attributed. Why `scene5_run`,
   `s5_9980_cell_door_coco3` and `pr_copy_from_clean` are undefined in those runners' file lists is unknown;
   they are Karateka-side gaps and out of scope here.
4. **Scene6 renders were captured at a fixed +240 frames** with my own loader. A different frame, or the
   drivers' own `*_live.lua` viewers, could show different content — the capture is a sample, not a sweep.
5. **`run_princess_gate*` / `sprite_engine_princess` were run but not verdict-checked** — they are
   build-and-stage scripts that print a live-view command; they have no automated pass/fail by design.

---

### 8 — Follow-up candidates

1. **Merge `hal-sync-fixes` → `wip`** (fast-forward, Jay's action). Then `wip → main` when that line is ready.
2. **Fix or retire the four broken runners** (`prod_boot_test`, `sprite_engine_sandbox`, `scene5_composite`,
   `r_boot_debugtrace`) — a suite where 4 of 21 cannot build is a degraded safety net, independent of this
   merge. `r_boot_debugtrace` needs only `execution_state="run"` in its Lua (idiom §12).
3. **Add verdict lines to the 10 silent tests**, or record explicitly that they are diagnostics rather than
   tests, so "green" means something uniform.
4. **Add `.bat` runners for the 4 `.sh`-only tests** if native-Windows parity is wanted — not needed for
   running them, but it would let one enumeration cover the whole suite.
5. Standing: the **§2A.3 authorship ruling** (now **eighteen** deferrals).

---

### 9 — User interaction during task

Three, all Jay-initiated and all load-bearing:
1. **"check if its stuck itbeen a long time"** — it was; 12.5 minutes on test 10. Diagnosed (MAME had exited;
   the runner's wrapper hung), cleared, and re-run. Jay's prompt is what surfaced it.
2. **"so the binary you showed me stops part way through the text scroll scene and doesn't show the princess
   imprisonment scene"** — correct observation, and it drove a real finding: prod boot is *documented* to halt
   at the scene-1→scene-2 cut, and scene6 (the imprisonment content) is **not linked into the prod binary at
   all**. Not a regression; the binary is byte-identical to `main`'s.
3. **"so all the visuals loked good. and merging to wip makes sense…"** — the 25.3 gate confirmation (§5) plus
   the merge-target decision, which corrected my mis-framing (§6.3). Jay then instructed: **"no write the
   report"** — so no merge was performed.

---

### 10 — Candidate(s) captured this task

`seeds/POP/live/2026-07-26-measure-divergence-against-the-merge-target-not-a-distant-ancestor.md` — *"When
assessing what a change proposal actually contains, measure it against the branch you are proposing to merge
INTO, not against a distant reference — divergence from the wrong baseline manufactures alarming numbers that
describe unrelated history rather than the proposal."* Fresh single-instance row (`initiator: clyde`); no
existing entry read or edited.

Arose from §6.3: I reported the review branch as "129 commits / 225 files ahead" and treated it as a merge
blocker across several turns. Against the branch's actual parent it is a 1-commit fast-forward of 6 files.
The number was arithmetically correct and decision-relevant in the wrong direction.

---

### 11 — Commit
`<hash>` — pushed to POP `origin/wip`. **Karateka: no commit, no merge; `main` @ `5eb92b1`, branch @ `3bd6feb`.**

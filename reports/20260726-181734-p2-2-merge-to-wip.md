## Form B Report — P2.2-merge — merge `hal-sync-fixes` → Karateka `wip` (Jay-authorized), mirror to POP, verify
**Class:** BUILD/INTEGRATION. Karateka `wip` (merge target, Jay-authorized) + POP `wip` (mirror).
**Karateka `main` UNTOUCHED at `5eb92b1`** — confirmed before and after.
**Prod byte-identity:** `88eba89b15cdf17c8d25e082d2d3e1f3cce57d38` throughout — before flag 3, after flag 3,
and after the merge. Not one byte moved.

### 0 — Receipt / status (C-35 stamp)
t0=2026-07-26T18:17:34Z (POP HEAD `ec09c56`, wip; tracked clean).
Karateka at receipt: `wip` `23c0592`, `hal-sync-fixes` `3bd6feb`, **`main` `5eb92b1`**, tracked clean.

**CLAUDE.md §2G conflict:** quoted in full in the P2.2 report §0a and unchanged here — §2G's "never modify
Karateka" targets *automatic* sync; this is the explicit, Jay-authorized Karateka-side task it names as the
legitimate route, targeting `wip` not `main`.

---

### 1 — Summary

**All four gates passed, flag 3 was added, the merge is done, POP is mirrored, and both repos verify green.**

| | |
|---|---|
| FF precondition | **clean** — `wip` an ancestor of the branch; 1 ahead, 0 behind |
| Flag 3 | **was ABSENT** → added (§3.2), prod binary **byte-identical** (hard-stop 10.4 clear) |
| Merge | **fast-forward, no merge commit** — `wip` `23c0592` → **`c101093`**, pushed |
| `main` | **`5eb92b1`, untouched** ✔ |
| POP mirror | `hal.inc` flag 3 mirrored; flags 1 & 2 already aligned from P2.2 |
| Karateka verify | build COMPLETE; 6/6 informative tests PASS; 6/6 scene6 drivers run |
| POP verify | build COMPLETE; P1.1 PASS; P1.3 PASS |

**Flag 3 required §2b: the declarations were not on the branch.** P2.2 investigated flag 3 but was
report-only, so nothing had been written. I added the four declarations to `hal.inc`, taking each signature
**from `gfx.s` itself rather than inventing it** — so the contract now records the real args, ZP usage and
constraints (the byte-aligned-only limitation on `_masked`/`stencil_punch`, the 16-bit physical row and #80
stride on `_scroll`, the `mix_*` ZP band on `_mixed`). The block also records *why* they are public contract
(25 call sites, `memory-window-audit.md` marking two LIVE, the content `opacity.s` files naming them as their
format contract), so the finding does not have to be re-derived.

**The one intentional divergence, and why it is not a fork.** POP's `gfx.s` keeps the P2.1 dormancy guard
around the four blit functions; Karateka's does not. That is correct in both directions: PA.6 ruled the
runtime blit INFEASIBLE for POP and P1.3 replaced it with compiled sprites, while P2.2-verify found Karateka
calls all four from engine code. Same flag content in both copies, different per-project configuration —
governance rule 3, not a fork.

**A quiet finding worth recording:** POP's `sys.s` and `mem.s` showed as differing from Karateka's, which
would have looked like drift. They are **content-identical**; only the line endings differ, because POP's
`.gitattributes` pins `*.s` to LF while Karateka's tree is CRLF. Any future cross-repo alignment check must
normalise EOL or it will report false drift on every `.s` file.

---

### 2 — Files modified

**Karateka (branch `hal-sync-fixes`, then fast-forwarded into `wip`):**
- `src/hal.inc` — flag 3: four entry-point declarations (+53 lines). Commit `c101093`.
- (already on the branch from P2.2: `sys.s`, `gfx.s`, `mem.s`, `entry.s`, `build.bat` — flags 1 & 2.)

**POP (`wip`):**
- `src/hal.inc` — flag 3 mirrored. Commit `dc5b52f`.
- `reports/20260726-181734-p2-2-merge-to-wip.md` — this report.

**Not modified:** Karateka `main` (never checked out for write, never pushed); POP `src/hal/coco3-dsk/*`
(flags 1 & 2 already correct from P2.2); engine code; `oracle/source/`.

---

### 3 — Reasoning

#### 3.1 — Preconditions (§5.1), all four hard-stops cleared

```
local  wip 23c0592   branch 3bd6feb   main 5eb92b1   tracked-modified 0
remote wip 23c0592   branch 3bd6feb   main 5eb92b1
git merge-base --is-ancestor origin/wip origin/hal-sync-fixes  -> YES
merge-base 23c0592 (= wip itself)    wip..branch 1    branch..wip 0
```
Hard-stop 10.2 (not a clean FF) and 10.3 (`main` moved) both clear.

#### 3.2 — Flag 3 (§2b ran): declarations added, proven inert

Checked first: all four **ABSENT** from `hal.inc` on the branch. P2.2 established the finding but made no
edit, correctly — the ruling was Jay's to give.

Signatures were read out of `gfx.s`'s own function headers, not composed from the names. The result matches
the file's existing declaration style (comment block + `import`, as used for `HAL_gfx_blit_sprite_opaque` and
`HAL_gfx_present`).

**Hard-stop 10.4 — declarations must not change the built binary:**
```
before: 88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
after : 88eba89b15cdf17c8d25e082d2d3e1f3cce57d38   -> BYTE-IDENTICAL
```
Doubly expected: P2.3-recon established `hal.inc` is not included in the `--decb` build at all — its `import`
directives are object-target-only, so `lwasm` cannot process them in an absolute build. **Flag 3 is therefore
contract *documentation* today, and becomes contract *enforcement* only if POP's linked-build conversion
happens.** Worth stating plainly so nobody assumes the declarations are checked by the toolchain.

Informative tests re-run on the branch with flag 3 present: **6/6 PASS**.

#### 3.3 — The merge (§5.3)

`git merge --ff-only` — refuses to create a merge commit by construction, so the "no merge commit" guarantee
is enforced by the tool, not by inspection afterwards. Confirmed anyway: `c101093` has a **single parent**
(`parents=3bd6feb`).

`wip` and `hal-sync-fixes` were both pushed (they now point at the same commit); **`main` was neither checked
out for write nor pushed.**

#### 3.4 — The POP mirror (§5.4), with a superset check before overwriting

Before overwriting POP's `hal.inc` I ran the `CLAUDE.md §2D`-style superset diff: **0 lines lost, +53 added**,
all of them flag 3. Only then did I copy, normalising to LF for POP's `.gitattributes`. Verified
content-identical to Karateka's afterward.

`sys.s`/`mem.s` needed no action — content-identical already (§1). `gfx.s` keeps its POP-local guard.

#### 3.5 — Post-merge verification (§5.5)

Karateka `wip`: build COMPLETE, prod `88eba89b…`; six informative tests PASS; six scene6 drivers all load at
`exec=$0200` and are still executing at +240 frames with PCs `$021C`, `$021F`, `$0238`, `$0CC3`, `$0333`,
`$0381` — **identical to the pre-merge run**, which is the strongest available evidence that execution is
unchanged.

POP `wip`: build COMPLETE (`hal.bin` 395 B), P1.1 probe PASS, P1.3 compiled-sprite proof PASS (492 bytes /
1968 px identical).

---

### 4 — Verification (AC-by-AC)

- **AC1 — FF precondition verified. MET.** §3.1. `--ff-only` used, so a merge commit was impossible.
- **AC2 — flag 3 present before merge; prod binary byte-identical. MET.** §3.2. Was absent; added; binary
  unchanged; informative tests green before committing.
- **AC3 — `wip` fast-forwarded + pushed; `main` still `5eb92b1`. MET.** §5, remote listing.
- **AC4 — POP mirrored, alignment noted. MET.** §3.4. Flag content identical; `gfx.s` dormancy guard is a
  declared rule-3 configuration difference, not a fork.
- **AC5 — both repos verified post-merge. MET.** §3.5. Karateka build + 6 informative + 6 scene6; POP build +
  P1.1 + P1.3. **No visual surfaced — no render changed** (identical scene6 PCs, byte-identical prod binary),
  which is what §7/25.3 specifies.
- **AC6 — no `main` merge, no force-push, no unrelated change. MET.** §5.

---

### 5 — Verdict-time evidence (v0.7 §11)

**25.1 — preconditions (verbatim):**
```
  local  wip : 23c0592   hal-sync-fixes : 3bd6feb   main : 5eb92b1   tracked-modified : 0
  YES — origin/wip is an ancestor of origin/hal-sync-fixes => clean FF possible
  merge-base 23c0592   wip..branch 1   branch..wip 0
```

**25.1 — flag 3 absent, then inert (verbatim):**
```
  HAL_gfx_blit_scroll              ABSENT — §2b must run
  HAL_gfx_blit_sprite_masked       ABSENT — §2b must run
  HAL_gfx_blit_sprite_mixed        ABSENT — §2b must run
  HAL_gfx_blit_stencil_punch       ABSENT — §2b must run
  ...
  before: 88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
  after : 88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
  -> BYTE-IDENTICAL — declarations are inert to codegen, as required
```

**25.1 — informative tests with flag 3 (verbatim):**
```
run_gfx_init_precheck        exit=0    RESULT: PASS
run_gfx_init_test            exit=0    MAME TEST: PASS
run_kernel_dispatch_test     exit=0    MAME TEST: PASS
run_sys_init_test            exit=0    RESULT: PASS
run_timer_framesync_test     exit=0    MAME TEST: PASS
run_vbl_irq_test             exit=0    MAME TEST: PASS
```

**25.1 — THE MERGE (verbatim):**
```
  on wip @ 23c0592
  Updating 23c0592..c101093
  Fast-forward
   build.bat               |  3 ++-
   src/engine/entry.s      | 18 +++++++++++++
   src/hal.inc             | 70 ++++++++++++++++++++++++++++++++++++++++++++++++-
   src/hal/coco3-dsk/gfx.s |  6 ++++-
   src/hal/coco3-dsk/mem.s |  6 ++++-
   src/hal/coco3-dsk/sys.s |  4 ++-
   6 files changed, 102 insertions(+), 5 deletions(-)
   create mode 100644 src/engine/entry.s
  merge exit=0

  c101093  parents=3bd6feb   <- SINGLE parent: no merge commit
  To github.com:Jsearle01/karateka-coco3.git
     23c0592..c101093  wip -> wip
```

**25.1 — `main` untouched, post-merge (verbatim):**
```
  c101093…  refs/heads/hal-sync-fixes
  5eb92b1…  refs/heads/main            <- UNCHANGED
  c101093…  refs/heads/wip
  local main: 5eb92b1  -> UNTOUCHED ✔      tracked-modified: 0
=== BUILD COMPLETE ===   88eba89b15cdf17c8d25e082d2d3e1f3cce57d38
```

**25.1 — POP mirror, superset-checked (verbatim):**
```
  POP hal.inc -> Karateka hal.inc:  +53  -0
  lines POP would LOSE: 0   <- must be 0 (superset check)
  content-identical to Karateka wip (EOL-normalised): True

  file                          K eol   P eol   same ignoring EOL?
  src/hal.inc                   CRLF    LF      NO — real content delta   (flag 3)
  src/hal/coco3-dsk/sys.s       CRLF    LF      YES                       (already aligned)
  src/hal/coco3-dsk/mem.s       CRLF    LF      YES                       (already aligned)
  src/hal/coco3-dsk/gfx.s       CRLF    CRLF    NO — POP dormancy guard   (POP-LOCAL, rule 3)
```

**25.1 — POP verification (verbatim):**
```
  build/loop_probe.bin (159 bytes)    build/hal.bin (395 bytes)    === BUILD COMPLETE ===
  [run_probe_test] PASS
  framebuffer_matches_cel  PASS all 492 bytes (1968 px) identical    [run_compiled_test] PASS
```

**25.1 — scene6 post-merge, PCs identical to pre-merge (verbatim):**
```
scene6_stage1_driver       snapshot at +240 frames (PC=$021C)
scene6_stage2_driver       snapshot at +240 frames (PC=$021F)
scene6_stage3_driver       snapshot at +240 frames (PC=$0238)
scene6_climb_crawl_driver  snapshot at +240 frames (PC=$0CC3)
scene6_walk_scrollA_driver snapshot at +240 frames (PC=$0333)
scene6_b2prime_driver      snapshot at +240 frames (PC=$0381)
```

**25.2 — Karateka `wip` @ `c101093` (pushed); `main` @ `5eb92b1` (untouched); POP mirror `dc5b52f`.**

**25.3 — N/A, by the dispatch's own condition.** §7 specifies a visual only if a render changed. Nothing
changed: the prod binary is byte-identical and every scene6 driver reached the identical PC at the identical
frame. The 13 PNGs from P2.2-verify — which Jay confirmed "looked good" — remain the current renders.

---

### 6 — Reactive deviations

1. **§2b ran** — flag 3 was absent, so this dispatch added it rather than merely merging. Anticipated by the
   dispatch, noted because it means the merged commit set is **2 commits, not 1** (`3bd6feb` + `c101093`).
2. **`hal-sync-fixes` was pushed too**, not just `wip`, so the remote branch reflects what was merged rather
   than a stale `3bd6feb`. Both now point at `c101093`. Not required by §9; done so the branch is not
   misleading if anyone inspects it.
3. **Flag 3's declarations are documentation, not enforcement** (§3.2). `hal.inc` is not assembled in the
   `--decb` build, so nothing checks them today. Surfaced because "declared in the contract" naturally reads
   as "enforced by the build", and here it is not.
4. **POP's `sys.s`/`mem.s` differ from Karateka's by line endings only.** Reported because a naive
   byte-comparison across the repos reports drift on every `.s` file and would send a future check chasing
   nothing.
5. **Scene6 verification reused P2.2-verify's snapshot loader** in `/c/karateka-capture/tools/` — the staging
   dir, not either repo. No `run_scene6_*` runners exist.

---

### 7 — Uncertainty flags

1. **`main` is now two merges behind a diverged line.** `main` (`5eb92b1`) and `wip` (`c101093`) have an old
   merge-base (`e29dd29`), with 18 commits on `main` the `wip` line lacks. The eventual `wip → main` merge is
   **not** a fast-forward and will need real reconciliation. That is a separate Jay gate, but it is a larger
   job than this one was, and nothing in this dispatch reduced it.
2. **Only the 6 informative tests were re-run after flag 3**, not the full 17. Justified by the binary being
   byte-identical and `hal.inc` being unassembled, but it is a subset.
3. **Four Karateka runners remain broken on both branches** (`prod_boot_test`, `sprite_engine_sandbox`,
   `scene5_composite`, `r_boot_debugtrace`) — pre-existing per P2.2-verify, untouched here. The suite's safety
   net is degraded independent of this merge.
4. **The scene6 "identical PC" evidence is strong but not a render diff.** Same PC at the same frame implies
   the same execution path; it does not byte-compare the framebuffers. The prod binary's byte-identity is the
   stronger claim.
5. **POP's HAL is still not exercised at runtime.** P2.1 §7.1 stands: POP's probes carry their own inline GIME
   setup and do not call `HAL_gfx_init`. POP's "build + P1.1 + P1.3 green" means the HAL assembles and nothing
   regressed — not that POP's adopted HAL runs.

---

### 8 — Follow-up candidates

1. **The `wip → main` reconciliation** (§7.1) — a real merge, not a fast-forward, with 18 commits on `main` to
   reconcile. Jay's gate; worth scoping before it is attempted.
2. **Fix or retire the four broken runners** (§7.3). `r_boot_debugtrace` needs only `execution_state="run"` in
   its Lua (idiom §12).
3. **Re-point a POP probe at the HAL entry points** (§7.5) — converts "assembles and doesn't regress" into
   "POP's HAL actually runs". Still the cheapest real proof outstanding from P2.1.
4. **Decide whether `hal.inc` should become enforceable** (§3.2 / P2.3-recon §8.4) — under the guarded
   one-source pattern it could be a real includable contract. That is the OS-kernel decision.
5. Standing: the **§2A.3 authorship ruling** (now **nineteen** deferrals).

---

### 9 — User interaction during task
**None during execution.** Jay's merge authorization and the flag-3 DECLARE ruling arrived via the dispatch;
Jay's visual confirmation was recorded in P2.2-verify §9.

---

### 10 — Candidate(s) captured this task

**None.** The methodology lessons available here — superset-check before overwriting a shared file, normalise
EOL before cross-repo comparison, prove a declaration is inert by binary identity — are either already
project practice or too narrow to be worth a pool row. The one genuinely new observation (a contract file
whose declarations nothing enforces) is already captured as
`2026-07-26-a-shared-interface-file-that-never-assembles-cannot-constrain-anything` from P2.2; this dispatch
is a second instance of it, and the pool's rule is fresh single-instance rows with folding left to the
reconciler — a duplicate row adds noise without adding information here.

---

### 11 — Commit
**Karateka:** `c101093` on **`wip`** (fast-forward from `23c0592`) and on `hal-sync-fixes`, both pushed.
**`main` @ `5eb92b1` — untouched.**
**POP:** `dc5b52f` (mirror) + `ff226ee` (this report), pushed to POP `origin/wip`.
